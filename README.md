# AI Virtual Mouse 🖐️

A Python project that lets you control the computer mouse using hand gestures via webcam. Built to use computer vision (MediaPipe / OpenCV) for gesture recognition and use those gestures to move/click.

---

## ✅ Features

- Real-time hand tracking using the webcam  
- Mapping gestures → mouse cursor movements and click actions  
- Gesture-based controls (e.g. index finger movement, pinch for click, etc.)  
- Cross-platform (should work on any OS with Python, webcam, and required libraries)  

---

## 🛠 Technologies Used

- Python  
- OpenCV  
- MediaPipe  
- PyAutoGUI (or similar library for controlling mouse)  
- NumPy  

---

## ⚙️ Installation

To get this project running locally:

```bash
git clone https://github.com/Aditya-Kumar-Singh-007/AI_Virtual_Mouse.git
cd AI_Virtual_Mouse
pip install -r requirements.txt

